{
  "name": "ro-sync-desktop",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node ./scripts/prepare.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "cargo check --manifest-path ./src-tauri/Cargo.toml",
    "test": "cargo test --manifest-path ./src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
