{
  "name": "serve",
  "title": "rosync serve",
  "category": "Daemon",
  "description": "Runs the local HTTP/WebSocket daemon that bridges the Studio plugin, filesystem watcher, and CLI.",
  "usage": "rosync serve --project <path> [--port <port>] [--game-id <id>] [--group-id <id>] [--place-id <id>...]",
  "examples": [
    "rosync serve --project . --port 7878",
    "rosync serve --project . --port 7878 --game-id 1234567890"
  ],
  "notes": [
    "This is the normal CLI-only entrypoint; the widget can also launch it for an active project.",
    "Run it manually when using Ro Sync without opening the widget interface."
  ]
}
